<div>
  <h4>Comment by <%= comment.author_name %></h4>
  
  <p class="comment"><%= comment.body %>
  
  <% if comment.created_at != nil  %>  
    <footer class="comment">Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later</footer>
  <% end %>

  </p>

</div>